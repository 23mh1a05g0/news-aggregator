

services:
  news-aggregator:
    build: .
    container_name: news-aggregator

    ports:
      - "5173:5173"

    healthcheck:
      test: ["CMD", "wget", "--spider", "http://localhost:5173"]
      interval: 30s
      timeout: 10s
      retries: 3